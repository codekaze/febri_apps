name: flutterx
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  google_sign_in: ^5.2.3
  get: ^4.5.1

  clipboard: ^0.1.3
  intl: ^0.17.0
  hive: ^2.0.0
  google_fonts: ^2.1.0
  url_launcher: ^6.0.6
  file_picker: ^3.0.1
  file_picker_cross: ^4.3.2
  # Remove flutter image compress if image compression is works!
  flutter_image_compress: ^1.0.0
  image_compression: ^1.0.3
  path_provider: ^2.0.1
  uuid: ^3.0.4
  ionicons: ^0.1.2
  percent_indicator: ^3.4.0

  glassmorphism_ui: ^0.2.0
  screenshot: ^1.2.3
  universal_html: ^2.0.8
  # fireverse:
  fireverse:
    path:
      ./../fireverse

  # UI KIT Packages
  titled_navigation_bar: ^4.1.0
  cuberto_bottom_bar: ^1.2.0
  google_nav_bar: ^5.0.5
  faker: ^2.0.0
  fancy_bottom_navigation: ^0.3.3
  flutter_rating_bar: ^4.0.0
  flutter_colorpicker: ^1.0.3

  syncfusion_flutter_charts: ^19.3.56
  bitsdojo_window: ^0.1.1+1
  flutter_inappwebview: ^5.3.2
  introduction_screen: ^2.1.0
  flutter_custom_clippers: ^2.0.0
  flutter_map: ^0.14.0
  latlong2: ^0.8.1
  geolocator: ^8.0.3
  permission_handler: ^8.3.0
  flutter_html: ^2.1.5
  flutter_spinkit: ^5.1.0
  flutter_slidable: ^1.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

dependency_overrides:
  get: 
    git:
      url: https://github.com/denyocrworld/getx_dark_knight

flutter:
  uses-material-design: true
  assets:
    - assets/icon/
    - assets/audio/
    - assets/document/
    - assets/image/
    - assets/image/gradient/
    - assets/video/
    - assets/font/
